interface:
  display_name: "Audit Feature"
  short_description: "Cross-check a feature spec against its source intake"
  default_prompt: "Use $audit-feature F-XXX to verify source traceability and find missed, drifted, or untraced requirements."
policy:
  allow_implicit_invocation: false
